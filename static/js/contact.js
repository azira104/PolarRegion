var contactUsUrl = document.getElementById('contact-us-url').getAttribute('data-url');
        
        // Now you can use the contactUsUrl variable in your JavaScript code
        console.log(contactUsUrl);